import { LoggerInterface } from '../common/LoggerInterface';
import { LoggerConfiguration } from './GSNConfigurator';
export declare function createClientLogger(loggerConfiguration?: LoggerConfiguration): LoggerInterface;
